<h2>Users</h2>


<%= link_to 'ユーザ編集画面', edit_user_path(current_user) %>

<% @users.each do |user| %>
  <%= attachment_image_tag user, :image %>
  <%= user.name %>
  <%= link_to 'Show', user_path(user) %>
<% end %>